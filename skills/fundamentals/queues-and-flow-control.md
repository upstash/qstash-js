# Queues

## Overview

This file covers FIFO queue management with configurable parallelism in QStash.

## Topics to include:

- Creating queues with `client.queue()`
- Enqueuing messages with `enqueueJSON()`
- Queue parallelism configuration
- Queue states and lifecycle
- Pausing and resuming queues
- DLQ behavior for queues

> **Note:** For controlling message processing rates and concurrency, see [advanced/flow-control.md](../advanced/flow-control.md)
