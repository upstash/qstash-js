# Message Deduplication

## Overview

This file covers preventing duplicate message processing using deduplication features.

## Topics to include:

- Content-based deduplication with `contentBasedDeduplication` parameter
- ID-based deduplication with `deduplicationId` parameter
- Deduplication window and timeframe
- When to use each deduplication method
- Deduplication with queues and schedules
- Best practices for preventing duplicates
