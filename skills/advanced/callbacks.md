# Callbacks

## Overview

This file covers success and failure callbacks for QStash messages.

## Topics to include:

- Setting success callback URLs with `callback` parameter
- Setting failure callback URLs with `failureCallback` parameter
- Callback payload structure and headers
- When callbacks are triggered
- Handling callback requests
- Best practices for callback endpoints
